meta {
  name: Rotate Relying Party Secret
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/api/admin/relying-parties/moi-portal/rotate-secret
  body: none
  auth: bearer
}

auth:bearer {
  token: {{admin_access_token}}
}
