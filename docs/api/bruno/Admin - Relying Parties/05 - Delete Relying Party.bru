meta {
  name: Delete Relying Party
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/api/admin/relying-parties/moi-portal
  body: none
  auth: bearer
}

auth:bearer {
  token: {{admin_access_token}}
}
