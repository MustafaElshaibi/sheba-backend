meta {
  name: Mark Payment Complete (mock)
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/api/requests/payments/REPLACE_WITH_PAYMENT_ORDER_ID/complete
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

docs {
  Any authenticated principal; ownership enforced in the handler. Mock payment-gateway
  callback — production PSP integration is deferred (T-PAY-1).
}
